[distutils]
index-servers =
  pypi
  testpypi

[pypi]
repository = https://upload.pypi.org/legacy/
username = __token__
password = ${PYPI_API_KEY}

[testpypi]
repository = https://test.pypi.org/legacy/
username = __token__
password = ${PYPI_TEST_API_KEY}
